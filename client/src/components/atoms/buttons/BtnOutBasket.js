import React from 'react'
import styles from '../../../style.js'

const BtnOutBasket = () => {
  return (
    <button className={`w-[36px] h-[19px] ${styles.flexCenter} text-sm font-light border-solid border-[1px] border-secondary rounded-sm`}>OUT</button>
  )
}

export default BtnOutBasket